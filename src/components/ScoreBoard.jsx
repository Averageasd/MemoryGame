export const ScoreBoard = ({score, maxScore}) => {
    return (
        <>
            <h2>
                Score : {score}
            </h2>
            <h2>
                Max Score: {maxScore}
            </h2>
        </>
    )
}